services:
  silence-on-retrojoue:
    image: ppsoj
    env_file:
      - .env
    volumes:
      - ~/data/ppsoj/media:/app/media
      - ~/data/ppsoj/static:/app/staticfiles
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 127.0.0.1:8010:8000
    depends_on:
      postgresdb:
        condition: service_healthy
  postgresdb:
    image: postgres:16
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 30s
      retries: 5

volumes:
  postgres_data:
